<?xml version="1.0" encoding="utf-8"?>
<!--
  A history card says four things, and each one has exactly one place:
  the poster, where you stopped (over the bar that shows it), what the title
  is, and which episode/source it came from. Everything used to be stacked on
  the poster at once — a two-line red badge across the top, a stray date at the
  bottom — so the artwork was unreadable and the card said less, not more.
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="160dp"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:background="@drawable/item_movie_history_bg"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:padding="8dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/cardView"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        app:cardCornerRadius="8dp"
        app:layout_constraintDimensionRatio="2.8:4"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:foreground="@drawable/fg_imageview_item_movie">

            <ImageView
                android:id="@+id/coverImage"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/joker" />

        </FrameLayout>

        <!--
          A state, not a label. It only appears when you are at the end of
          something, which is the one thing worth putting over the artwork.
          Capped and single-line so a long word can never wrap it into a block.
        -->
        <TextView
            android:id="@+id/movieStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start|top"
            android:layout_marginStart="6dp"
            android:layout_marginTop="6dp"
            android:background="@drawable/background_text_movie_status"
            android:ellipsize="end"
            android:fontFamily="@font/rubik_regular"
            android:gravity="center"
            android:maxLines="1"
            android:maxWidth="120dp"
            android:paddingHorizontal="8dp"
            android:paddingVertical="1dp"
            android:textColor="#fff"
            android:textSize="@dimen/tv_text_caption"
            android:textStyle="bold"
            tools:text="Almost finished" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:orientation="vertical"
            android:paddingBottom="3dp">

            <TextView
                android:id="@+id/info_item"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="@font/rubik_regular"
                android:maxLines="1"
                android:paddingHorizontal="8dp"
                android:paddingBottom="3dp"
                android:shadowColor="#000000"
                android:shadowRadius="4"
                android:textColor="#fff"
                android:textSize="@dimen/tv_text_caption"
                android:textStyle="bold"
                tools:text="00:13 of 1:48:53" />

            <androidx.core.widget.ContentLoadingProgressBar
                android:id="@+id/progressBar"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="3dp"
                android:layout_gravity="bottom|center"
                android:layout_marginHorizontal="8dp"
                android:indeterminate="false"
                android:max="100"
                android:progressDrawable="@drawable/custom_progress"
                tools:progress="20" />
        </LinearLayout>

    </androidx.cardview.widget.CardView>

    <!-- The name a person recognises: the show, never the episode label. -->
    <TextView
        android:id="@+id/movieTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="2dp"
        android:layout_marginTop="6dp"
        android:ellipsize="marquee"
        android:fontFamily="@font/rubik_regular"
        android:marqueeRepeatLimit="marquee_forever"
        android:maxLines="1"
        android:textColor="#ffffff"
        android:textSize="@dimen/tv_text_label"
        app:layout_constraintEnd_toEndOf="@+id/cardView"
        app:layout_constraintStart_toStartOf="@+id/cardView"
        app:layout_constraintTop_toBottomOf="@+id/cardView"
        tools:text="The Odyssey" />

    <!-- Which episode, from which source. Hidden entirely when it is neither. -->
    <TextView
        android:id="@+id/timeStr"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="2dp"
        android:layout_marginTop="1dp"
        android:ellipsize="end"
        android:fontFamily="@font/rubik_regular"
        android:maxLines="1"
        android:textColor="@color/lb_grey"
        android:textSize="@dimen/tv_text_caption"
        app:layout_constraintEnd_toEndOf="@+id/movieTitle"
        app:layout_constraintStart_toStartOf="@+id/movieTitle"
        app:layout_constraintTop_toBottomOf="@+id/movieTitle"
        tools:text="Episode 1 · VidAPI" />

    <LinearLayout
        android:id="@+id/playContainer"
        android:layout_width="0dp"
        android:layout_height="35dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/background_button"
        android:duplicateParentState="true"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/timeStr">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:duplicateParentState="true"
            android:fontFamily="@font/rubik_regular"
            android:text="@string/play"
            android:textColor="@color/color_item_tv_category_tv"
            android:textSize="@dimen/tv_text_label" />

        <androidx.appcompat.widget.AppCompatImageView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginHorizontal="4dp"
            android:duplicateParentState="true"
            app:srcCompat="@drawable/lb_ic_play"
            app:tint="@color/color_item_tv_section_profile" />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
