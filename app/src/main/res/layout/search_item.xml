<?xml version="1.0" encoding="utf-8"?>
<!--
  The card fills whatever column width the grid hands it, instead of pinning
  itself to 140dp: a fixed item inside a wider cell left ragged gaps between
  results. The poster holds a 2:3 ratio so every row lines up, and the title
  sits under it rather than on top of the artwork, which was unreadable over
  bright covers.
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:background="@drawable/search_item_focused"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:padding="5dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/moviePosterContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:cardCornerRadius="8dp"
        app:cardElevation="2dp"
        app:layout_constraintDimensionRatio="H,2:3"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/moviePoster"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/joker"
                app:shapeAppearance="@style/ShapeAppearance.Material3.Corner.Medium" />

            <View
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_gravity="bottom"
                android:background="@drawable/gradient_overlay_bottom" />

            <TextView
                android:id="@+id/movieSource"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|start"
                android:layout_margin="6dp"
                android:background="@drawable/source_chip_bg"
                android:ellipsize="end"
                android:maxLines="1"
                android:paddingHorizontal="6dp"
                android:paddingVertical="1dp"
                android:textColor="@color/netflix_white"
                android:textSize="@dimen/tv_text_caption"
                android:visibility="gone"
                tools:text="AnimePahe"
                tools:visibility="visible" />

        </FrameLayout>
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/movieTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:lines="2"
        android:textColor="@color/netflix_text_secondary"
        android:textSize="@dimen/tv_text_label"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/moviePosterContainer"
        tools:text="Anime Title" />

</androidx.constraintlayout.widget.ConstraintLayout>
