<?xml version="1.0" encoding="utf-8"?>
<!-- Deliberately narrow and bottom-anchored: a settings menu sits beside the
     control it belongs to, and should never take the picture away from you. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="360dp"
    android:layout_height="wrap_content"
    android:background="@drawable/dialog_background"
    android:orientation="vertical"
    android:paddingHorizontal="14dp"
    android:paddingVertical="14dp">

    <TextView
        android:id="@+id/popupTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:layout_marginStart="4dp"
        android:fontFamily="sans-serif-medium"
        android:textColor="@color/netflix_white"
        android:textSize="@dimen/tv_text_subtitle" />

    <androidx.leanback.widget.VerticalGridView
        android:id="@+id/popupList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxHeight="300dp"
        android:clipToPadding="false"
        android:overScrollMode="never"
        android:scrollbars="none"
        app:focusOutEnd="true"
        app:focusOutFront="true"
        app:numberOfColumns="1" />

</LinearLayout>
